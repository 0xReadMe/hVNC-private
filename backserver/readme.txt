—борка сервера:
VS2013, конфигураци€ Release Win32.

Ќастройка серверной машины:
1. ”станавливаем mysql (туда сервер пишет инфу об онлайн клиентах, которую можно подт€нуть из любого внешнего сервиса).
  - сервер может работать с удаленным сервером базы на любом хосте, но надо понимать, что при отсутствии св€зи между серверами возможна рассинхронизаци€ списка онлайн клиентов.
2. —оздаем базу скриптом db/schema.sql (можно сменить пользовател€ и пароль дл€ работы сервиса - дл€ него создаетс€ отдельный пользователь с локальным доступом только к данной базе).
3.  опируем файлы сервера: backserver.exe и backserver.ini 
  - в ини можно указать порт, который будет слушать сервер - лучше пользоватьс€ стандартными 80 или 443, которые открыты у большинства клиентов
  - в ини надо указать параметры дл€ подключени€ к созданной базе
4. ѕрописываем запуск сервера в виде сервиса Windows (backserver -install или средствами sc create) - сервис будет запускатьс€ автоматически с запуском операционной системы.
5. Ќастраиваем файрвол дл€ доступа к сервису снаружи.
6. ¬се готово, запускаем сервис (net start backserver или из ƒиспетчера задач).
